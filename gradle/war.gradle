apply plugin: "war"

bootWar {
    mainClassName = "com.cours.MicroserviceJobHistoryApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
